{
  "name": "config-driven-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/sync-config.js",
    "dev": "vite --host 0.0.0.0 --port 3000",
    "prebuild": "node scripts/sync-config.js",
    "build": "vite build",
    "prepreview": "node scripts/sync-config.js",
    "preview": "vite preview --host 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {}
}
